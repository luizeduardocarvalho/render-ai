# Copy to terraform.tfvars (gitignored) and fill in.
billing_account   = "XXXXXX-XXXXXX-XXXXXX" # gcloud billing accounts list
app_project_id    = "studioia-app"
backup_project_id = "studioia-backups"
state_bucket_name = "studioia-tfstate"
org_id            = "678908172272" # the studioia.app organization (gcloud organizations list)
