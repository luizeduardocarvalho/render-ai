You are editing an existing photorealistic architectural render. Change ONLY the areas
marked below and leave everything else exactly as it is.

=== INPUT IMAGES ===
IMAGE 1: The render to edit.
IMAGE 2: The same render with the areas to change filled in solid colors at partial
  opacity. The colors are labels only; do not paint the colors themselves.

=== WHAT TO CHANGE ===
{{ range .Regions }}- {{ .Color }} area (region {{ .Number }}): {{ .Instruction }}
{{ end }}
=== HOW TO CHANGE IT ===
- Inside each colored area, make the change described for it, and nothing else.
- Blend the new content into the scene: match its perspective, scale, lighting direction,
  color temperature, shadows, reflections, grain and sharpness, so it looks photographed
  together with the rest of the room.
- Where an instruction changes an object's color or material (a wall, a floor, a fabric),
  keep the object's shape, edges, texture detail and the light falling on it.

=== EVERYTHING ELSE STAYS IDENTICAL (highest priority) ===
- OUTSIDE the colored areas, reproduce IMAGE 1 exactly: every object, wall, window, door and
  piece of furniture, and every color, material, light and shadow.
- Keep the EXACT camera position, framing, perspective, focal length and aspect ratio of
  IMAGE 1. Do NOT crop, zoom, pan, rotate or extend the frame.
- Do NOT add, remove, move, resize or restyle anything that is not inside a colored area.

Output the complete edited image.
